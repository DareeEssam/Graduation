import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.graduation.R

class RegisterActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_choosetype)

       // val therapistButton: Button = findViewById(R.id.therapist)
       // val userButton: Button = findViewById(R.id.user)

        //therapistButton.setOnClickListener {

           // val intent = Intent(this, TherapistActivity::class.java)
            //startActivity(intent)
       // }

        //userButton.setOnClickListener {

           // val intent = Intent(this, UserActivity::class.java)
            //startActivity(intent)
        //}
    }
}
